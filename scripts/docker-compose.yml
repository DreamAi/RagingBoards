version: "3.9"
services:
  api:
    image: python:3.11
    working_dir: /app
    volumes:
      - .:/app
    command: uvicorn backend.main_api:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
